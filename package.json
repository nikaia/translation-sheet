{
    "name": "nkreliefdev-translation-sheet",
    "version": "1.4.5",
    "private": true,
    "scripts": {
        "release-patch-dryrun": "standard-version --release-as patch --no-verify --dry-run",
        "release-patch": "standard-version --release-as patch --no-verify",
        "release-minor": "standard-version --release-as minor --no-verify",
        "release-major": "standard-version --release-as major --no-verify"
    },
    "dependencies": {
        "standard-version": "^8.0.1",
        "lodash": "^4.17.19",
        "dot-prop": "^4.2.1"
    }
}
